{
  "name": "mindwtr-cloud",
  "license": "AGPL-3.0-only",
  "private": true,
  "version": "1.2.1",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "lint": "eslint -c ../../eslint.node.config.mjs . --max-warnings 0",
    "start": "bun run src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "TZ=UTC LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 bun test",
    "test:coverage": "TZ=UTC LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 bun test --coverage --coverage-reporter=lcov"
  },
  "dependencies": {
    "@mindwtr/core": "workspace:*"
  }
}
