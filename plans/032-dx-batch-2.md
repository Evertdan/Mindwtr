# Plan 032: DX batch — workspace dependency, mobile sync trace, verify gate, module slices

> Drift check: `git diff --stat b0a96ccc9..HEAD -- apps/desktop/package.json package.json bun.lock apps/desktop/src/lib/sync-service.ts apps/mobile/lib/sync-service.ts packages/core/src/task-utils.ts apps/mobile/components/inbox-processing/useInboxProcessingController.ts`

## Status
- **Priority**: P2 · **Effort**: M · **Risk**: MED (lockfile) · **Depends on**: none, but land DX-01 FIRST and alone (lockfile isolation) · **Category**: dx/tech-debt
- **Planned at**: `b0a96ccc9`, 2026-08-22

## Findings (one commit each)

1. **DX-01/DEPS-01 `file:` → `workspace:*`** — `apps/desktop/package.json` declares `"@mindwtr/core": "file:../../packages/core"` (only member not on workspace:*); bun materializes a REAL 138MB copy at `apps/desktop/node_modules/@mindwtr/core`, 23 entries diverged from source today, second lockfile entry at `bun.lock:3296`; caused false test failures twice (duplicate-React trap + today's closure round). All three build configs already alias to `../../packages/core/src`, so the dep is dead weight. FIX: change to `workspace:*`; `rm -rf apps/desktop/node_modules` (whole dir — it contains only @mindwtr); `bun install` at root; READ the bun.lock diff (the deferral reason was lockfile churn — confirm no unrelated resolution moved; if the diff shuffles unrelated packages, STOP and report the diff summary); confirm the nested path is now a symlink or absent; run `cd apps/desktop && bun run test`, `bun run typecheck`, and a desktop production build (`cd apps/desktop && bun run build` — Tauri; if the full bundle is too slow, `bunx vite build` proves the resolution story). Release note: none (dev-only). Do NOT `bun update` anything.
2. **DX-03 wire mobile's sync payload trace** — core declares optional `tracePayload` port (`sync-run-ports.ts:160`) and emits at every stage; desktop implements (`apps/desktop/src/lib/sync-service.ts:2775`), mobile never mentions it — yet mobile is where sync debugging is hardest. The ~155-line trace builders (`sync-service.ts:355-508`: buildSyncPayloadSurfaceTraceExtra, collectChangedTracePaths, buildCollectionDiffTraceSample, buildSyncPayloadDiffTraceExtra) are pure (no Tauri/DOM refs — verified by auditor grep; re-verify). FIX: hoist to `packages/core/src/sync-payload-trace.ts`, re-export from index.ts (metro barrel rule — mobile imports MUST be barrel-reachable), desktop imports instead of defining, mobile passes tracePayload gated on the same `settings.diagnostics.loggingEnabled` flag desktop uses. Trace strings must keep desktop's existing redaction (ids/field names, never content — #854). Red test: core unit test on the moved formatters (port desktop's existing coverage); mobile test asserting the port is wired when diagnostics on.
3. **DX-04 verify gate lacks Rust** — root `package.json:90` verify = typecheck+lint+test+governance+i18n+schema+docs-readme; `native:test` (`:107`) and `test:perf` absent. FIX: TIME a warm `bun run native:test` first; if ≤ ~90s warm, append to `verify`; else add `verify:full` including it + `test:perf` and leave `verify` as-is; update the CONTRIBUTING description of the gate in the same commit (repo-local file — check whether the docs-parity gate covers it before editing; repo CONTRIBUTING.md is NOT in the web-docs parity set). `bun run test:governance` must pass after.
4. **DEBT-03 task-speech slice** — move `SpeechResultLike`/`SpeechUpdatePlan`/`buildTaskUpdatesFromSpeechResult` (~140 lines, `packages/core/src/task-utils.ts:1271-1412`) to `packages/core/src/task-speech.ts` — the test file `task-speech.test.ts` ALREADY has that name and imports from ./task-utils. Re-export from index.ts (barrel baseline `index-exports.test.ts` must stay green — it fails loudly if reachability breaks). Update the four external call sites' imports only if they import the symbols directly from a subpath (they import from the barrel — verify; if barrel, no call-site change needed). NO wholesale task-utils split (settled: churn vs 23 importers).
5. **DEBT-04 mobile inbox copilot memo duplication** — `useInboxProcessingController.ts:285-306` two byte-identical 11-line memo bodies differing only in pool/selection. FIX: one `rankTokenSuggestions(pool, selected, terms, limit)` helper (local to the file or the module dir) + trivial test; exhaustive-deps is ERROR in mobile — dependency arrays exact. Do NOT promote desktop's parsing helpers to core in this plan (behaviors must be diffed first — record as follow-up if they match).

## Verification
Per finding as above; end state: root `bun run verify` green, mobile+desktop suites green.

## STOP
- DX-01: bun.lock diff shows unrelated resolution changes.
- DX-03: any trace builder turns out to reference desktop-only globals.
